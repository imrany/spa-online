// @flow strict
// import { useParams } from "react-router-dom"
function Profile() {
    // const {email}=useParams()
    return (
        <div>
            
        </div>
    );
};

export default Profile;