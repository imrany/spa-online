// @flow strict
import { useParams } from "react-router-dom"
function Dashboard() {
    const {email}=useParams()
    return (
        <div>
            
        </div>
    );
};

export default Dashboard;